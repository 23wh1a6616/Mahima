Calculator (Implemented in Java) :  It allows users to perform arithmetic operations like addition, subtraction, multiplication, and division. It typically takes 
user input for numbers and the desired operation, then displays the result. The program can be implemented using conditional statements or a switch-case structure.

Snake Game (Implemented in Java):  The Snake game in Java is a simple graphical game where the player controls a snake that grows longer as it eats food. The goal
is to avoid hitting walls or the snake’s own body. The game can be developed using Java's Swing or JavaFX libraries for rendering the game interface and handling user
input for controlling the snake's movement.

Vector Embeddings (Implemented in Python using GUI Interface): Created a chatbot using GUI where the user could upload a document and ask any query. The chatbot 
checks for similarity index using Sentence-BERT model and returns the answer appropriately. 
